spring:
  application:
    name: vote-service
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/vote?createDatabaseIfNotExist=true&useSSL=true
    username: vote
    password: vote@123
  jpa:
    hibernate.ddl-auto: update
    generate-ddl: true
    show-sql: true
  rabbitmq: 
    host: localhost
    port: 5672
    username: guest
    password: guest
    
rabbitmq: 
  exchange: vote.exchange
  routing-key: vote.routing.key
  queue-name: voteQueue

user:
  info:
    url: https://user-info.herokuapp.com/users/%s